<form method="post" action="{{ path('chanson_delete', {'id': chanson.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chanson.id) }}">
    <button class="btn">Delete</button>
</form>
