{% block stylesheets %}
<link href="{{ asset('css/form.css') }}" rel="stylesheet">
{% endblock %}

{% block body %}
    <div class="page-wrapper bg-gra-02 p-t-20 p-b-50 font-poppins">
        <div class="wrapper wrapper--w680">
            <div class="card card-4">
                <div class="card-body">
                    <h1 class="title">Créer un nouveau Client</h1>

                    {{ include('client/_form.html.twig') }}
                </div>
            </div>
            <a class="go-back" href="{{ path('client_index') }}">⮜ Retour à la liste</a>
        </div>
    </div>
    
    <script src="{{ asset('js/myscript.js') }}"></script>
{% endblock %}
