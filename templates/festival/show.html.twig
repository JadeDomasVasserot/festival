{% block body %}
    <h1>Festival</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ festival.id }}</td>
            </tr>
            <tr>
                <th>Nomfestival</th>
                <td>{{ festival.nomfestival }}</td>
            </tr>
            <tr>
                <th>Datedebut</th>
                <td>{{ festival.datedebut ? festival.datedebut|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ festival.lieu }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ festival.genre }}</td>
            </tr>
            <tr>
                <th>Nbplaces</th>
                <td>{{ festival.nbplaces }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('festival_index') }}">back to list</a>

    <a href="{{ path('festival_edit', {'id': festival.id}) }}">edit</a>

    {{ include('festival/_delete_form.html.twig') }}
{% endblock %}
