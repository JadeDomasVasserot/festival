{{ include('nav.html.twig')}}
{% block body %}
    <h1>Edit ChanteurFestival</h1>

    {{ include('chanteur_festival/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('chanteur_festival_index') }}">back to list</a>

    {{ include('chanteur_festival/_delete_form.html.twig') }}
{% endblock %}
