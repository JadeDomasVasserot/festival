<form class="spacing" method="post" action="{{ path('chanteur_festival_delete', {'id': chanteur_festival.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir déprogrammer ce chanteur ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chanteur_festival.id) }}">
    <button class="btn btn--radius-2 btn--blue">Supprimer</button>
</form>
