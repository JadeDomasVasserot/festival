{% extends 'base.html.twig' %}

{% block title %}ChansonChanteur index{% endblock %}

{% block body %}
    <h1>ChansonChanteur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for chanson_chanteur in chanson_chanteurs %}
            <tr>
                <td>{{ chanson_chanteur.id }}</td>
                <td>
                    <a href="{{ path('chanson_chanteur_show', {'id': chanson_chanteur.id}) }}">show</a>
                    <a href="{{ path('chanson_chanteur_edit', {'id': chanson_chanteur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('chanson_chanteur_new') }}">Create new</a>
{% endblock %}
