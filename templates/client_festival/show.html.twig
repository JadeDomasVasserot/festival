{% block body %}
    <h1>ClientFestival</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client_festival.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('client_festival_index') }}">back to list</a>

    <a href="{{ path('client_festival_edit', {'id': client_festival.id}) }}">edit</a>

    {{ include('client_festival/_delete_form.html.twig') }}
{% endblock %}
